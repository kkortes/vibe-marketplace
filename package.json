{
  "name": "vibe-marketplace",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run build && bunx live-server --port=3000",
    "build": "mkdir -p lib && cp -r node_modules/@ape-egg/vibe lib/vibe && cp node_modules/async-await-websockets/client.js lib/aaw.js"
  },
  "dependencies": {
    "@ape-egg/vibe": "^1.7.0",
    "async-await-websockets": "^3.0.5"
  }
}
